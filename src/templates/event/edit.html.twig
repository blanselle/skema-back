{% extends 'base.html.twig' %}

{% block title %}Modification évènement{% endblock %}

{% block body %}
    <div class="nav_actions">
        <a href="{{ path('event_index') }}" class="btn btn-secondary">Retour à la liste des évènements</a>
        {{ include('event/_delete_form.html.twig') }}
    </div>
    <hr />
    <h1>Modifier l'évènement {{ item.label }}</h1>

    <div class="save_container">
        {{ include('event/_form.html.twig') }}
    </div>
{% endblock %}
