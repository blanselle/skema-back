{% extends 'base.html.twig' %}

{% block title %}Nouveau niveau de bourse{% endblock %}

{% block body %}
    <div class="nav_actions">
        <a href="{{ path('scholar_ship_level_index') }}" class="btn btn-secondary">Retourner à la liste des niveaux de bourse</a>
    </div>
    <hr />
    <h1>Créer un niveau de bourse</h1>

    {{ include('scholarShipLevel/_form.html.twig') }}
{% endblock %}
