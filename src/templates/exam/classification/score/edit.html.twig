{% extends 'base.html.twig' %}

{% block title %}Modification d'un score{% endblock %}

{% block body %}
    <div class="nav_actions">
        <a href="{{ path('exam_classification_scores_index', {'id': examClassificationScore.examClassification.id}) }}" class="btn btn-secondary">Retour à la liste des scores</a>
        {{ include('exam/classification/score/_delete_form.html.twig') }}
    </div>
    <hr />
    <h1>Modifier le score pour la typologie {{ examClassificationScore.examClassification.name }}</h1>

    <div class="save_container">
        {{ include('exam/classification/score/_form.html.twig') }}
    </div>
{% endblock %}
