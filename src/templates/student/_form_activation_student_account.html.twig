<div class="form_container">
    <div class="row">       
        <div class="nav_actions">
            <a href="{{ path('student_account_activate', {'user': student.user.id}) }}" class="btn btn-primary justify-content-end">Activer immédiatement le compte</a>
        </div>
    </div>
</div>