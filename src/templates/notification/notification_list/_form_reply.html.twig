<div class="notification_reply_form">
{{ form_start(form) }}
    <div class="row">
        <div class="col">
            {{ form_row(form.subject) }}
        </div>
    </div>
    <div class="row">
        <div class="col">
            {{ form_row(form.content) }}
        </div>
    </div>

    <div class="save_container">
        <button class="btn btn-success">{{ button_label|default('Répondre') }}</button>
    </div>
{{ form_end(form) }}
</div>
