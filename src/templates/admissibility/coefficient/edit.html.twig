{% extends 'base.html.twig' %}

{% block title %}Modification d'un coefficient{% endblock %}

{% block body %}
    <div class="nav_actions">
        <a href="{{ path('admissibility_coefficient_index') }}" class="btn btn-secondary">Retour à la liste des coefficients</a>
        {% if coefficient is defined %}{{ include('admissibility/coefficient/_delete_form.html.twig') }}{% endif %}
    </div>
    <hr />

    {{ include('admissibility/coefficient/_form.html.twig') }}
{% endblock %}