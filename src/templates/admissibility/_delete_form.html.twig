<form method="post" action="{{ path('admissibility_conversion_delete', {'id': admissibility.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette table ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ admissibility.id) }}">
    <button class="btn btn-danger action-delete"><i class="bi bi-trash"></i></button>
</form>
