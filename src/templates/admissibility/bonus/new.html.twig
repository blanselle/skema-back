{% extends 'base.html.twig' %}

{% block title %}Ajouter un bonus{% endblock %}

{% block body %}
    <div class="nav_actions">
        <a href="{{ path('admissibility_bonus_index', {'categoryId': category.id}) }}" class="btn btn-secondary">Retour à la liste des bonus</a>
    </div>
    <hr />
    <h1>Ajouter un bonus de type {{category.name}}</h1>

    {{ include('admissibility/bonus/_form.html.twig') }}
{% endblock %}
