{% extends 'base.html.twig' %}

{% block title %}Modification campus{% endblock %}

{% block body %}
    <div class="nav_actions">
        <a href="{{ path('campus_index') }}" class="btn btn-secondary">Retourner à la liste des campus</a>
        {{ include('campus/_delete_form.html.twig') }}
    </div>
    <hr />
    <h1>{{ campus.name }}</h1>

    <div class="save_container">
        {{ include('campus/_form.html.twig') }}
    </div>
{% endblock %}