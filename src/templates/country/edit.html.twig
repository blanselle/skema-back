{% extends 'base.html.twig' %}

{% block title %}Modification Pays{% endblock %}

{% block body %}
    <div class="nav_actions">
        <a href="{{ path('country_index') }}" class="btn btn-secondary">Retour à la liste des pays</a>
        {{ include('country/_delete_form.html.twig') }}
    </div>
    <hr />
    <h1>Modifier le pays</h1>

    {{ include('country/_form.html.twig') }}
{% endblock %}